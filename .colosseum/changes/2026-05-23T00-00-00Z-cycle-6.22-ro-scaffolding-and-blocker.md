# Cycle 6.22 — RO scaffolding landed; 6.22.b blocked on `spec.Fintype` incompatibility

- Date: 2026-05-23
- Scope: new `proofs/lean/Specs/Quartz/Protocol/ProtocolVCVioROModel.lean` (cycle 6.22.a scaffolding) + in-file blocker documentation for 6.22.b/c
- Build status: `lake build` green at 2670 jobs (+1 file in tree; 0 new `sorry`)

## Cycle 6.22.a — landed

The substantive Option-(a) closure of (d-pigeonhole-impossible) needs a
random-oracle simulator alongside the cycle-6.13 honest-deterministic
one. Cycle 6.22.a delivers the scaffolding:

* New module `Specs/Quartz/Protocol/ProtocolVCVioROModel.lean` (167 lines
  of scaffolding + ~100 lines of blocker documentation).
* `commitHashROSim : QueryImpl CommitHashSpec (StateT _ ProbComp)` using
  VCV-io's `randomOracle`.
* `commitHashBytesROSim` analogous for `CommitHashBytesSpec`.
* Explicit `DecidableEq UserDataCommit`/`DecidableEq ByteSeq` via
  `Classical.decEq` (matching the cycle-6.14.a pattern, namespaced
  separately so they don't shadow the cycle-6.14.a `_root_` instances).
* Explicit `SampleableType (CommitHashSpec.Range t)` /
  `SampleableType (CommitHashBytesSpec.Range t)` instances that
  unfold the dependent range projection to `BitVec 512` and invoke
  VCV-io's `FinEnum.SampleableType` derivation.

Build clean. No new `sorry`. No regressions.

## Cycle 6.22.b — BLOCKED on VCV-io's `OracleSpec.Fintype` requirement

The intended deliverable was a theorem of the shape:

    commitHashCollisionAdvRO 𝒜 n ≤ (qb + 2)² / (2 · 2^512)

…proved via VCV-io's `probEvent_logCollision_le_birthday_total` from
`VCVio/OracleComp/QueryTracking/Birthday.lean:396`. That theorem's
file-level variable declaration (line 21) requires
`[spec.DecidableEq] [spec.Fintype] [spec.Inhabited]`.

`OracleSpec.Fintype spec` extends `PFunctor.Fintype` which requires
`Fintype` on BOTH the *index* type and the *range* type.

For `CommitHashSpec : OracleSpec UserDataCommit`:

* Range = `UserData = BitVec 512` — Fintype OK post cycle 6.18.
* Index = `UserDataCommit` — **Fintype FAILS**. `UserDataCommit` has
  `domainSep : DomainSep = List UInt8` and `contractAddr : Addr =
  String` fields, both infinite types post cycle 6.20.

This is a genuine incompatibility between two design choices that
were each correct in isolation:

* Cycle 6.21's carrier refinement chose `List UInt8`/`String` for
  variable-length fields to match production semantics faithfully.
  Production `domainSep` is `"QUARTZ-HS-V1".toUTF8.toList`; production
  `contractAddr` is a bech32 string. Both are inherently variable
  length.
* VCV-io's birthday bound was designed for fixed-size index types,
  the typical cryptographic setting where the adversary submits
  finite-domain inputs.

## Three remediation paths, all out of session scope

(a) **Bound the spec's index artificially**: introduce a parallel
    spec `BoundedCommitHashSpec` over `Vector UInt8 N × ... ×
    Vector UInt8 M` for fixed maxima. Prove the bound under the
    bounded spec. Connect to production via the observation that
    deployed `domainSep` (12 bytes) and `contractAddr` (≤ 64 bytes
    bech32) both fit comfortably. Loses generality over arbitrary
    deployments but is the cleanest local fix. **Estimated effort:
    2-3 days focused work. Recommended next step.**

(b) **Weaken VCV-io's bound to drop `[spec.Fintype]` on the index**:
    the birthday argument doesn't fundamentally need Fintype on the
    domain — only on the range. Sending an upstream PR to VCV-io
    would relax this. Cross-repo work; multi-week coordination.

(c) **Refactor cycle 6.21**: revert `DomainSep`/`Addr`/`ByteSeq` to
    fixed-width carriers (`BitVec`-based) at the cost of losing
    production correspondence on variable-length fields. Loses the
    cycle-6.21 win that the deployed `domainSep`/`addr` shape is
    faithfully modelled. Not recommended.

## What lands here vs what's queued

**Landed (this commit)**:
- Cycle 6.22.a scaffolding (working simulators + supporting instances).
- Comprehensive in-file blocker documentation, including the three
  remediation paths and recommended next step.
- New methodology v0.4 ask candidate: variable-length-domain birthday
  bounds. The right end state is upstream relaxation of the
  `spec.Fintype` requirement. Methodology should flag this whenever
  a spec's domain refines to `List`/`String`/other infinite carriers
  and a downstream collision-bound theorem is queued.

**Queued (out of this cycle)**:
- Cycle 6.22.b.1: path (a) bounded variant. Define
  `BoundedCommitHashSpec`, restate the bound, prove the forward
  implication "game wins → cache has collision", invoke
  `probEvent_logCollision_le_birthday_total`. 2-3 days focused work.
- Cycle 6.22.c (downstream lift migration to RO semantics):
  conditional on 6.22.b.1 landing. Migrates `pkOfUserData_commitHash`
  and the four lifts that consume `commitHash_inj` to probabilistic
  shapes. 1-2 weeks beyond 6.22.b.1.

## Round A attack #8 closure status (updated)

| Aspect | Status |
|---|---|
| Surface-side (def-tying) | Closed by cycle 6.15 |
| Substantive scaffolding (RO simulator) | Closed by cycle 6.22.a (this commit) |
| Substantive bound theorem | Blocked on `spec.Fintype`; three paths documented |
| Downstream lift migration | Conditional on bound theorem |

The bound's mathematical content is the standard textbook birthday
argument; the blocker is purely VCV-io's API surface, not a
cryptographic shortcoming.

## Why the bound statement isn't even type-checked here

An earlier draft of this module included the bound statement as a
`theorem ... := by sorry` with the intent of letting the type system
catch any signature mistakes. That draft failed type-checking at the
`Pr[...]` notation, which transitively requires
`HasEvalSPMF (OracleComp spec)`, which requires `spec.Fintype`. So
the bound statement itself cannot be written over the unbounded
`CommitHashSpec` — it must wait for one of the remediation paths.

This is the cleanest end state: the scaffolding stands, the blocker
is documented at the call site, and no `sorry` is introduced into
the tree.
