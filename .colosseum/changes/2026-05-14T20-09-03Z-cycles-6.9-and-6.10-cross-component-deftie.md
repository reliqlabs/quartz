# Cycles 6.9 + 6.10 — cross-component lifts def-tying (combined)

- Date: 2026-05-14
- Plan: `.colosseum/refactor-plan-vcvio-content.md` (Round A response)
- Scope: two lifts in `proofs/lean/Specs/Quartz/Protocol/ProtocolVCVioTriple.lean`:
  - `cross_component_transfers_conservation_negl` (6.9)
  - `cross_component_auction_winner_determinism_negl` (6.10)
- Build status: `lake build` green at 2667 jobs (unchanged from baseline)
- Round A attacks addressed for both: #1, #2, #3, #11
- Bundle correction: triple → single in both cases

## Why combined

Both lifts have the same structure: cross-component theorems whose classical proofs use only `handshake_sound` for the attestation-binding part (Groth16-soundness failure mode) plus *unconditional* derived theorems for the other conclusion parts. The change records would substantially duplicate; combining them is a hygiene call, not a methodology departure. Each lift's `def`s and theorem are independent and well-named; the commit hash references both.

## Analysis

### Cycle 6.9 — `cross_component_transfers_conservation`

Classical proof in `Conservation.lean:227-243` has conclusion `(∃ q signed ∧ projections) ∧ conservationInvariant b'`. The conservation part is unconditional via `applyTransferRequest_preserves_conservation` (a derived theorem, no axiom usage). The signed-quote part is the Groth16-soundness failure mode (transitively via `handshake_sound`).

Adversary output is `(HandshakeCheck × TransferRequest)`. The `b`, `b'`, `hInv`, `hApp` of the classical theorem are universally quantified inside the win predicate **but elided from the win event** because the conservation conclusion cannot fail.

### Cycle 6.10 — `cross_component_auction_winner_determinism`

Classical proof in `AuctionDeterminism.lean:189-209` has conclusion `winner-eq ∧ price-eq ∧ (∃ q signed ∧ projections)`. Parts 1 and 2 are unconditional via `h_canon : claimed = resolveAuction round` (by congruence). Part 3 is the Groth16-soundness failure mode.

Adversary output is `(HandshakeCheck × AuctionRound × ResolveMessage)`. Win predicate retains all three hypotheses (`h_raw`, `h_round`, `h_canon`) and the full negated conclusion; the forward-implication proof uses `h_canon` to discharge parts 1 and 2, leaving Part 3 as the Groth16 break.

## Changes (each lift)

Both cycles add the same structural pieces:
- `transfersConsWinPred` / `auctionDetermWinPred`
- `reduce_transfers_to_groth` / `reduce_auctionDeterm_to_groth`
- `noncomputable def consFailAdv` / `noncomputable def auctFailAdv`
- `transfersConsWinPred_imp_groth16SoundnessWinPred_projected` / `auctionDetermWinPred_imp_groth16SoundnessWinPred_projected`

The lift theorems both go from ~11 free parameters with an assumed three-summand bound down to 2 parameters (adversary + Groth16-negligibility on the derived adversary). Proof structure: `negligible_of_le` + `probEvent_bind_pure_comp` + `probEvent_mono`.

## Pattern summary so far (cycles 6.4–6.10)

| Cycle | Lift | Bundle (was → is) | Surviving failure mode |
|---|---|---|---|
| 6.4 | `verifyGroth16_yields_decoded_negl` | single → single | Groth16 |
| 6.5 | `handshake_sound_negl` | dual → single | Groth16 |
| 6.6 | `handshake_binds_ecies_key_negl` | triple → single | Groth16 |
| 6.7 | `session_confidentiality_negl` | triple → zero | None (deterministic-only) |
| 6.8 | `session_confidentiality_via_extractor_negl` | triple → zero | None (deterministic-only) |
| **6.9** | `cross_component_transfers_conservation_negl` | **triple → single** | Groth16 |
| **6.10** | `cross_component_auction_winner_determinism_negl` | **triple → single** | Groth16 |

Seven cycles in, every intermediate lift has been over-bundled in the original Step 6.0–6.3 work. Five had a single Groth16 surviving failure mode; two were degenerate-zero. The terminal lift (cycle 6.11, `cross_component_session_bind_negl`) is the only remaining intermediate, and it's the load-bearing terminal that the original plan claimed needed 4 distinct axiom groups. The cycle-6.4-through-6.10 pattern suggests the terminal lift's actual bundle count may also be smaller than 4, but the analysis there requires care because Round A specifically flagged the terminal as the place where the disjunction-decomposition discipline matters.

## Round A attacks closed for both lifts

- #1 (free-symbol tautology): `consFailAdv` and `auctFailAdv` are `def`s
- #2 (Trojan h_bound): bounds proven via `probEvent_mono`
- #3 (no reduction): `reduce_*_to_groth` are concrete
- #11 (over-quantified): each theorem takes one adversary + one hypothesis

Same as cycles 6.4–6.6, `commitHashE` and `tdxVerifier` records remain in the closure (substrate). The `groth16Verifier.sound` projection is shadowed by `h_groth_negl`. The `commitHashE.injective` projection appears in the closure of cycle 6.9's lift (via `pkOfUserData_commitHash` → wait, does cycle 6.9 actually use `pkOfUserData_commitHash`? No — the transfers proof goes through `handshake_sound`, not `handshake_binds_ecies_key`, so it doesn't touch `pkOfUserData_commitHash`. The `commitHashE` axiom may not appear in 6.9's closure at all. Same likely for 6.10. Worth checking separately.)

## Build status

`lake build` 2667 jobs green (unchanged from baseline). All downstream lifts in Triple and Quad still compile.

The convenience packagings (`*Fail_secure_of_triple_bundle_secure`, `*Game_secure_of_triple_bundle_secure`) for both lifts remain unchanged — they are generic over `SecurityExp` / `SecurityGame` summand decompositions.

## Forward path

Cycle 6.11 is the terminal `cross_component_session_bind_negl` in `ProtocolVCVioQuad.lean`. The original plan classified it as a 5-summand union bound (the load-bearing quad-bundle terminal). Per the cycle-6.4-through-6.10 pattern, the actual bundle count may be smaller — but Round A specifically flagged the terminal as the place where disjunction-decomposition discipline matters. The cycle should:

1. Analyze the classical `cross_component_session_bind` proof in `CrossComponent.lean` for actual axiom consumption.
2. Build the win predicate, reduction(s), and content-bearing advantage.
3. If the terminal lift's bundle count really is 4 (Groth16 KS + circuit-equiv + TDX + hash), produce a multi-summand reduction (different shape from cycles 6.4–6.10).
4. Update the methodology-v0.2 PR materials to reflect what the terminal-lift analysis actually requires.

Expected effort 2–3 days (longest cycle in the plan).
