*~
.DS_Store
*.manifest
*.manifest.sgx
*.sig
*.hash
*.height
*light-client-proof.json
*output
target/
artifacts/
.secrets/
.env
.env.*
**/.env.local

#cli
crates/cli/quartz.toml
.cache/

# Build results
/artifacts
target/
schema/

# Cargo+Git helper file (https://github.com/rust-lang/cargo/blob/0.44.1/src/cargo/sources/git/utils.rs#L320-L327)
.cargo-ok

# Text file backups
**/*.rs.bk

# IDEs
*.iml
.idea
.vscode

# Apalache working dirs (created by `quint verify`)
_apalache-out/

# Lean build output
.lake/

# Verus invocation artifacts: when `verus path/to/foo.rs` is run from the
# repo root, it drops a Mach-O binary at ./foo alongside the .rs source.
# These are build artifacts and must not be committed.
/attested
/encryption
/instantiate
/key_manager
/session_create
/session_set_pub_key
